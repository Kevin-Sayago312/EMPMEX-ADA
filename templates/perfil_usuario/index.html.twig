{% extends 'base.html.twig' %}

{% block title %} {{parent()}} - Mi Perfil {% endblock %}

{% block body %}

{{parent()}}

<!-- breadcrumb-section -->
<div class="breadcrumb-section breadcrumb-bg">
	<div class="container">
		<div class="row">
			<div class="col-lg-8 offset-lg-2 text-center">
				<div class="breadcrumb-text">
					<p>Tu perfil</p>
					<h1>Toda tu información</h1>
				</div>
			</div>
		</div>
	</div>
</div>
<!-- end breadcrumb section -->

<!-- EMPIEZA APARTADO DE INFORMACION DEL USUARIO -->
<div class="config-perfil"> 
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card">
                <div class="card-header">
                    <h3 class="text-center">Mi Perfil</h3>
                </div>
                <div class="card-body">
                    <ul class="list-group">
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Información Personal
                            <a href="{{path('app_informacion_personal')}}" class="btn btn-primary btn-sm"><i class="fas fa-arrow-right"></i></a>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Direcciones
                            <a href="{{path('app_direcciones')}}" class="btn btn-primary btn-sm"><i class="fas fa-arrow-right"></i></a>
                        </li>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Compras
                            <a href="{{path('app_compras')}}" class="btn btn-primary btn-sm"><i class="fas fa-arrow-right"></i></a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
<!-- TERMINA APARTADO DE INFORMACION DEL USUARIO -->

{% endblock %}
